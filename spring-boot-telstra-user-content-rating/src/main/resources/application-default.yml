spring:
  application:
    name: user-contentrating-service
server:
  port: 9013
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:9015/eureka}
  instance:
    preferIpAddress: true